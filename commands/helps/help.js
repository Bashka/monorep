const {red, yellow} = require('colors');

module.exports = `
${yellow('COMMANDS')}
    init --help - monorep init
    list --help - packages list
    info --help - package info
    up --help - packages version up
`;
